# KV-Embedding Prototype Dependencies
# Note: torch is pre-installed in the nvidia container
#
# Phase-1 (HuggingFace, legacy) pipeline only needs transformers + numpy etc.
# Phase-3 (TransformerLens) needs transformer_lens < 3.0: the 3.x release
# removed the transformer_lens.past_key_value_caching module that the
# kvembed backend imports. Same pin as Dockerfile and experiments/nnm/
# exp17_requirements.txt.

transformers>=4.40.0,<5.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
accelerate>=0.27.0
bitsandbytes>=0.41.0
beir>=2.0.0
qdrant-client>=1.10.0
transformer_lens>=2.18.0,<3.0
